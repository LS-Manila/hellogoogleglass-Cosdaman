package com.indooratlas.android.sdk.indoornavigation.glassfunc;

import com.indooratlas.android.sdk.indoornavigation.R;

/**
 * Created by Cosda on 9/21/2016.
 */
public class GlassMenuSwitch {

    //TODO THIS IS THE FUNCTION FOR SWITCH CASE

    public static void getFloorNumber (int FloorOption) {

       /* switch (FloorOption) { //switch case for buildings

            case "Lambda":



                switch (item.getItemId()) {     //switch case for floors
                    case R.id.Lambda_Floor_1:

                        floorNumber = 1;

                        switch (item.getItemId()) {

                            case R.id.L101:
                                intentQR.putExtra("SCAN_MODE", "QR_CODE_MODE");
                                startActivityForResult(intentQR, 0);
                                roomNumber = 1;
                                break;

                            case R.id.L109:
                                intentQR.putExtra("SCAN_MODE", "QR_CODE_MODE");
                                startActivityForResult(intentQR, 0);
                                roomNumber = 9;
                                break;

                        }

                    case R.id.Lambda_Floor_2:

                        floorNumber = 2;

                        switch (item.getItemId()) {
                            case R.id.L201:
                                intentQR.putExtra("SCAN_MODE", "QR_CODE_MODE");
                                startActivityForResult(intentQR, 0);
                                roomNumber = 1;
                                break;

                            case R.id.L202:
                                intentQR.putExtra("SCAN_MODE", "QR_CODE_MODE");
                                startActivityForResult(intentQR, 0);
                                roomNumber = 2;
                                break;

                        }
                }

            case R.id.Xi:
                //TODO no data on Xi building yet.

                break;
        } */
    }
}
